---
global:

route:
  group_by: [cluster, alertname]
  # group_by: ['alertname', 'cluster', 'service', 'job']
  group_interval: 2m
  group_wait: 1m # wait a minute for other alert before sending
  # repeat_interval: 720h # wait a month before resending alert
  repeat_interval: 1h
  receiver: high_alert_manager
  routes:
  # Send severity=slack alerts to slack.
  - match:
      severity: low
    receiver: slack_alert_manager_low
  - match:
      severity: medium
    receiver: slack_alert_manager_medium
  - match:
      severity: critical
    receiver: high_alert_manager

receivers:
- name: high_alert_manager
  slack_configs:
  - api_url: "https://hooks.slack.com/services/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"
    username: "Moonworkers / Exploitation / Staging"
    color: "danger"
    channel: '#test-alert-manager'
    # icon_emoji: ":panda_face:"
    title: "Moonworkers / Exploitation / Staging - {{ .CommonLabels.alertname }}"
    text: |-
      Hello Skalers,

      {{ range .Alerts -}}

      *Alert:* {{ .Annotations.summary }}
      *Description:* {{ .Annotations.message }}
      *Details:*
      {{ range .Labels.SortedPairs }}
      • *{{ .Name }}:* `{{ .Value }}`
      {{ end }}
      {{- if .Annotations.runbook_url }} :notebook: *<{{ .Annotations.runbook_url }}|Runbook>* {{ end }}

      {{ end }}

      You can find more information on: https://staging.jarvis.local
      Have a great day !"

    send_resolved: true

- name: slack_alert_manager_medium
  slack_configs:
  - api_url: "https://hooks.slack.com/services/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"
    username: "Moonworkers / Exploitation / Staging"
    color: "warning"
    channel: '#test-alert-manager'
    title: "Moonworkers / Exploitation / Staging - {{ .CommonLabels.alertname }}"
    text: |-
      Hello Skalers,

      {{ range .Alerts -}}

      *Alert:* {{ .Annotations.summary }}
      *Description:* {{ .Annotations.message }}
      *Details:*
      {{ range .Labels.SortedPairs }}
      • *{{ .Name }}:* `{{ .Value }}`
      {{ end }}
      {{- if .Annotations.runbook_url }} :notebook: *<{{ .Annotations.runbook_url }}|Runbook>* {{ end }}

      {{ end }}

      You can find more information on: https://staging.jarvis.local
      Have a great day !"

    send_resolved: true

- name: slack_alert_manager_low
  slack_configs:
  - api_url: "https://hooks.slack.com/services/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"
    username: "Moonworkers / Exploitation / Staging"
    color: "#439FE0"
    channel: '#test-alert-manager'
    title: "Moonworkers / Exploitation / Staging - {{ .CommonLabels.alertname }}"
    text: |-
      Hello Skalers,

      {{ range .Alerts -}}

      *Alert:* {{ .Annotations.summary }}
      *Description:* {{ .Annotations.message }}
      *Details:*
      {{ range .Labels.SortedPairs }}
      • *{{ .Name }}:* `{{ .Value }}`
      {{ end }}
      {{- if .Annotations.runbook_url }} :notebook: *<{{ .Annotations.runbook_url }}|Runbook>* {{ end }}

      {{ end }}

      You can find more information on: https://staging.jarvis.local
      Have a great day !"

    send_resolved: true